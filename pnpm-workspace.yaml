packages:
  - packages/*
  - apps/*

allowBuilds:
  browser-tabs-lock: true
  electron: true
  electron-winstaller: true
  esbuild: true
  msw: true
  node-pty: true
  sharp: true
  workerd: true

# dbus-next only uses usocket as an optional accelerator. Its native ArrayBuffer
# allocation is incompatible with Electron's V8 sandbox; Node's built-in Unix
# socket implementation is portable and fully supports the session bus here.
ignoredOptionalDependencies:
  - usocket

catalog:
  vite: npm:@voidzero-dev/vite-plus-core@0.1.13
  vite-plus: 0.3.0
  vitest: npm:@voidzero-dev/vite-plus-test@0.1.13

overrides:
  vite: 'catalog:'
  vitest: 'catalog:'
  react: '19.2.6'
  react-dom: '19.2.6'

peerDependencyRules:
  allowAny:
    - vite
    - vitest
  allowedVersions:
    vite: '*'
    vitest: '*'
